(ns {{project}}.models.{{plural}})

(defn build [] {})

(defn all
  [& [page per-page]]
  [{:id 1}])

(defn find-one
  [id]
  {:id 1})

(defn create
  [{{singular}}]
  1)

(defn update-fields
  [id fields]
  {})

(defn destroy
  [id]
  {})
